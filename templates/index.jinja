{% extends 'base.jinja' %}

{% block content %} 
<div class="container mt-5">
    <h1 class="display-4">Hello! Welcome to the Friends's Portal!</h1>
    <hr class="my-4">
    <p>Get started by creating an account or logging in to access all features.</p>
    <p class="lead">
        <a class="btn btn-primary btn-lg" href="{{ url_for('signup') }}" role="button">Sign Up</a>
        <a class="btn btn-secondary btn-lg" href="{{ url_for('login') }}" role="button">Login</a>
    </p>
</div>
{% endblock %}
